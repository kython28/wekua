pub const errors = error {
    TensorIsnotComplex,
    InvalidValue,

    InvalidScalarDtype,
    UnsupportedDataType,

    InvalidCoordinates,
    InvalidBuffer,

    UnqualTensorsDtype,
    UnqualTensorsDimension,
    UnqualTensorsShape,
};
